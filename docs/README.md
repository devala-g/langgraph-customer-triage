# docs/

Place `langsmith-trace.png` here once you've captured a LangSmith trace screenshot from a real run of the demo. The README references this path; until the file exists the image will show as broken.

Suggested screenshot: open https://smith.langchain.com → `customer-triage-demo` project → pick a billing-ticket run → expand the full trace tree → capture the whole panel at retina resolution.
